import React from 'react';
import { Typography } from '@mui/material';

const SchedulePage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Train Schedules</Typography>
      <Typography>TODO: Implement schedule display</Typography>
    </div>
  );
};

export default SchedulePage;